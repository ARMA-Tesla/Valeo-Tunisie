#if !defined(__LANG_TRANSLATION_H__)
#define __LANG_TRANSLATION_H__

/* language support */
static SLanguageItem PANELlang[] = {
	P(PANEL,						_noop("IMAQ Panel")),
	{0 , NULL, 0}
};

static SLanguageItem MAINlang[] = {
	P(MAIN,							_noop("Vision Config")),
	V(MAIN_CAMERA_TXT,				_noop("Camera")),
	V(MAIN_IMAGE_TXT, 				_noop("Image")),
	V(MAIN_CONFIG_TXT,				_noop("Config")),
	V(MAIN_TASK_TXT,				_noop("Task")),
	{0 , NULL, 0}
};

static SLanguageItem CAMCFGlang[] = {
	P(CAMCFG,						_noop("Camera Config")),
	V(CAMCFG_SETTINGS_TXT,			_noop("Settings")),		   
	L(CAMCFG_SHUTTER, 				_noop("Shutter Speed")),
	L(CAMCFG_GAIN, 					_noop("Gain")),
	L(CAMCFG_BRIGHTNESS,			_noop("Brightness")),
	L(CAMCFG_FRAME_RATE,			_noop("Frame Rate")),
	L(CAMCFG_SNAP_AVERAGE, 			_noop("Image Avarage")),
	L(CAMCFG_FRATE, 				_noop("Frame Rate")),
	{0 , NULL, 0}
};
	
static SLanguageItem PNLTASKSlang[] = {
	P(PNLTASKS,						_noop("Configure Task")), 
	V(PNLTASKS_STEP_TXT, 			_noop("Step Type")),
	V(PNLTASKS_NAME_TXT, 			_noop("Task Name:")),	
	{0 , NULL, 0}
};
	
static SLanguageItem PNLSTEPSlang[] = {
	P(PNLSTEPS,						_noop("Task/Steps")),
	V(PNLSTEPS_IMAGE_TXT,			_noop("oreginal image")),
	{0 , NULL, 0}
};
	
static SLanguageItem PANPATTlang[] = {
	P(PANPATT,						_noop("Pattern Matching")),
	V(PANPATT_NAME_TXT, 			_noop("Step Name:")),
	V(PANPATT_PATTERN_TXT, 			_noop("Pattern")),
	V(PANPATT_SEARCH_TXT, 			_noop("Search Conditions")),
	L(PANPATT_SREGION, 	  			_noop("Search Region")),
	L(PANPATT_NUM_MATCHES, 			_noop("Number of matches")),
	L(PANPATT_MIN_SCORE, 			_noop("Match Score")),
	L(PANPATT_MIN_CONTRAST, 		_noop("Contrast")),
	L(PANPATT_MATCH_MODE, 			_noop("Mode")),
	L(PANPATT_BKTREE, 				_noop("Result")),
	L(PANPATT_PREVIEW, 				_noop("Preview")),
	{0 , NULL, 0}
};

static SLanguageItem PANGPATTlang[] = {
	P(PANGPATT,						_noop("Golden Template")), 
	V(PANGPATT_NAME_TXT, 			_noop("Step Name:")),
	V(PANGPATT_PATTERN_TXT, 		_noop("Pattern")),
	V(PANGPATT_SEARCH_TXT, 			_noop("Search Conditions")),
	L(PANGPATT_SREGION, 	  		_noop("Search Region")),
	L(PANGPATT_MIN_SCORE, 			_noop("Match Score")),
	L(PANGPATT_MIN_CONTRAST, 		_noop("Contrast")),
	L(PANGPATT_MATCH_MODE, 			_noop("Mode")),
	L(PANGPATT_BKTREE, 				_noop("Result")),
	L(PANGPATT_PREVIEW, 			_noop("Preview")),
	L(PANGPATT_BRIGHT_LEVEL, 		_noop("Bright Level")),
	L(PANGPATT_DARK_LEVEL, 			_noop("Dark Level")),
	{0 , NULL, 0}
};
	
static SLanguageItem PANHISTlang[] = {
	P(PANHIST,						_noop("Histogram")),
	V(PANHIST_NAME_TXT,				_noop("Step Name:")),
	V(PANHIST_HISTOGRAM_TXT,		_noop("Histogram")), 
	L(PANHIST_BKTREE,				_noop("Result")),
	L(PANHIST_SREGION,				_noop("Histogram Region")),
	{0 , NULL, 0}
};
	
static SLanguageItem PANMATHlang[] = {
	P(PANMATH,						_noop("Math Operations")),
	V(PANMATH_NAME_TXT,			  	_noop("Step Name:")),
	V(PANMATH_IMAGE_TXT,			_noop("Source Image")), 
	V(PANMATH_SETTINGS_TXT,		  	_noop("Settings")),  
	L(PANMATH_CONSTANT,			  	_noop("Constant")), 
	L(PANMATH_PREVIEW, 				_noop("Preview")),   
	{0 , NULL, 0}
};


static SLanguageItem IMGBUFFERlang[] = {
	P(IMGBUFFER,					_noop("Image to Buffer")),
	{0 , NULL, 0}
};

static SLanguageItem PANRESlang[] = {
	P(PANRES,						_noop("Resample")), 
	V(PANRES_NAME_TXT,			  	_noop("Step Name:")), 
	V(PANRES_SETTINGS_TXT,		  	_noop("Settings")),
	L(PANRES_CONSTANT,			  	_noop("Constant")),
	L(PANRES_INTERPOL,			  	_noop("Interpolation")),
	L(PANRES_PREVIEW, 				_noop("Preview")),
	{0 , NULL, 0}
};
	
static SLanguageItem CFGlang[] = {
	P(CFG,							_noop("Config")),  
	L(CFG_SPATH,					_noop("Template Path")),
	{0 , NULL, 0}
};
	
static SLanguageItem MAKEBUFFlang[] = {
	P(MAKEBUFF,						_noop("Images Buffer")),
	V(MAKEBUFF_NEW_TXT, 			_noop("New Buffer")),
	V(MAKEBUFF_BUFFERS_TXT, 		_noop("Buffer")), 
	{0 , NULL, 0}
};

static SLanguageItem PANFACElang[] = {
	P(PANFACE,						_noop("Panel Face Checking")), 
	V(PANFACE_NAME_TXT,			  	_noop("Step Name:")), 
	V(PANFACE_PATTERN_TXT, 			_noop("Selected Area Preview")),
	V(PANFACE_SELECTION_TXT, 		_noop("Selection")),
	L(PANFACE_AREA_SIZE,			_noop("Area Size:")),
	V(PANFACE_SELECTED_POINT_TXT_1,	_noop("Point 1:")),
	V(PANFACE_SELECTED_POINT_TXT_2,	_noop("Point 2:")),
	V(PANFACE_SELECTED_POINT_TXT_3,	_noop("Point 3:")),
	V(PANFACE_SELECTED_POINT_TXT_4,	_noop("Point 4:")),
	V(PANFACE_SELECTED_POINT_TXT_5,	_noop("Point 5:")),
	L(PANFACE_BKTREE,				_noop("Result")),
	L(PANFACE_PREVIEW, 				_noop("Preview")),   
	{0 , NULL, 0}
};

#endif
