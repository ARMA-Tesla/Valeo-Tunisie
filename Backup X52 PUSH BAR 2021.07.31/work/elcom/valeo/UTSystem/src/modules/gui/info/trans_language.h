#if !defined(__LANG_TRANSLATION_H__)
#define __LANG_TRANSLATION_H__

#include "info.h"

static SLanguageItem INFOlang[] = {   
 	P(PANEL,				_noop("Info")),
	V(PANEL_TEXT_VERSION,	_noop("Version:")),
	V(PANEL_TEXT_ADMIN,		_noop("Administrator:")),
	V(PANEL_TEXT_TIME,		_noop("Time:")),
	V(PANEL_TEXT_SHIFT,		_noop("Shift:")),
	V(PANEL_MSG_SHIFT,		_noop("none")),
	V(PANEL_MSG_ADMIN,		_noop("log out")),
	{0 , NULL, 0}
};

#endif    
