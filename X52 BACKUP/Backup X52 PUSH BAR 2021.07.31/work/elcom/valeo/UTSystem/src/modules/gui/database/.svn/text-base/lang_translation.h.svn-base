#if !defined(__LANG_TRANSLATION_H__)
#define __LANG_TRANSLATION_H__

#include "database.h"

/* language support pattern */
static SLanguageItem DBSlang[] = {
   	P(DBS,                  	_noop("Database")),
	L(DBS_USERNAME,				_noop("User:")),
	L(DBS_USERPRODUCT,			_noop("Tester:")),
	L(DBS_USERRIGHTS,			_noop("Rights:")),
	V(DBS_RIGHTS_TXT,           _noop("You have not required rights for browsing or editing database!")),
   	{0 , NULL, 0}
};

static SLanguageItem HISTlang[] = { 
	P(HIST,                  	_noop("History")),   
	L(HIST_SWITCH_TEST, 		_noop("Hide Tests")),
	L(HIST_SWITCH_TESTPARAM, 	_noop("Hide Tests Param.")),
	L(HIST_SWITCH_PROD, 		_noop("Hide Product")),
	L(HIST_SWITCH_PRODPARAM, 	_noop("Hide Product Param.")),
	L(HIST_SWITCH_PRODMASTER,   _noop("Hide Product Masters")),     
	L(HIST_SWITCH_USER,   		_noop("Hide Users")), 
	L(HIST_SWITCH_USERRIGHTS,	_noop("Hide Users Rights")),   
	L(HIST_MERGE, 				_noop("Merge History")),
	L(HIST_DTPICKER, 			_noop("Data from date:")),  
   	{0 , NULL, 0}
};

static SLanguageItem TESTlang[] = { 
	P(TEST,					_noop("Test")),
	L(TEST_STEP,			_noop("Step")),
	L(TEST_NAME,			_noop("Name")),
	L(TEST_DESCRIPT,		_noop("Description")),
   	{0 , NULL, 0}
};

static SLanguageItem PARAMlang[] = { 
	P(PARAM,				_noop("Parameter")),
	L(PARAM_NB,				_noop("Parameter")),
	L(PARAM_NAME,			_noop("Name")),
	L(PARAM_DESCRIPT,		_noop("Description")),
	L(PARAM_VALUE,			_noop("Value")),
	L(PARAM_VTYPE,			_noop("VType")),
   	{0 , NULL, 0}
};

static SLanguageItem PRODUCTlang[] = { 
	P(PRODUCT,				_noop("Product")),
	L(PRODUCT_PARENT,		_noop("Parent")),
	L(PRODUCT_NAME,			_noop("Name")),
	L(PRODUCT_DESCRIPT,		_noop("Description")),
	L(PRODUCT_SETTINGS,		_noop("Visible")),
   	{0 , NULL, 0}
};

static SLanguageItem INFOlang[] = {
	P(INFO,            		_noop("Database")),            
	L(INFO_DB_DELETE_HIST, 	_noop("Delete Param.History")),
	L(INFO_INI_LIMIT, 		_noop("Create Test List")),
	L(INFO_INI_PARAMETER, 	_noop("Create Param. List")),
	L(INFO_INI_USER,		_noop("Create User List")),
	L(INFO_TO_FILE, 		_noop("Result to file")),   
	L(INFO_DTPICKER_1,		_noop("History to date")),
	L(INFO_INI_LIMIT_HIST,	_noop("Create Test List from History")),
	L(INFO_INI_PARAMETER_HIST,	_noop("Create Param. List from History")),
	{0 , NULL, 0}
};

static SLanguageItem PRODUCT_CPlang[] = { 
	P(PRODUCT_CP,               _noop("Copy Struct")), 		
	V(PRODUCT_CP_SETTING_TXT, 	_noop("Copy Settings")), 
	L(PRODUCT_CP_SOURCE, 		_noop("Select Source")),
	L(PRODUCT_CP_LISTBOX, 		_noop("Selected Products")),
	L(PRODUCT_CP_SET_PROD_PARAM, 	_noop("Copy Product Parameters")),
	L(PRODUCT_CP_SET_PROD_MASTER, 	_noop("Copy Product Master")), 
	L(PRODUCT_CP_SET_TEST, 			_noop("Copy Tests")),
	L(PRODUCT_CP_SET_TEST_PARAM, 	_noop("Copy Test Parameters")),
	L(PRODUCT_CP_SET_LINE_PROCESS, 	_noop("Copy Line Processes")),
	L(PRODUCT_CP_SET_LINE_STEP, 	_noop("Copy Line Steps")),
	L(PRODUCT_CP_SET_LINE_PARAM, 	_noop("Copy Line Parameters")),
   	{0 , NULL, 0}
};

static SLanguageItem MASTERlang[] = {   
   P(MASTER,                  	_noop("Master Panel")),
   L(MASTER_MASTER_ID,			_noop("Master")),
   L(MASTER_MASTER_CODE,		_noop("Master Code")),
   {0 , NULL, 0}
};

static SLanguageItem DLG_USERlang[] = {   
   P(DLG_USER,                  _noop("User Dialog")),
   V(DLG_USER_DBS_TXT,     		_noop("Database Rights")),  
   V(DLG_USER_TESTER_TXT,  		_noop("Tester Rights")),  
   L(DLG_USER_NAME, 			_noop("__Name")),
   L(DLG_USER_PASSW1, 			_noop("__Password")), 
   L(DLG_USER_PASSW2, 			_noop("Password Repeat")),
   L(DLG_USER_RIGHTS,			_noop("Rights")),
   L(DLG_USER_DRIGHTS_1, 		_noop("View Data")), 
   L(DLG_USER_DRIGHTS_2, 		_noop("Edit parameters")),
   L(DLG_USER_DRIGHTS_3, 		_noop("Edit tests")),
   L(DLG_USER_DRIGHTS_4, 		_noop("Edit products")),
   L(DLG_USER_DRIGHTS_5, 		_noop("Edit users")),
   L(DLG_USER_TRIGHTS_1, 		_noop("Team Leader")),
   L(DLG_USER_TRIGHTS_2, 		_noop("Operator")),  
   L(DLG_USER_TRIGHTS_3, 		_noop("Quality Mode")),  
   L(DLG_USER_TRIGHTS_4, 		_noop("Maintenance Mode")),  
   L(DLG_USER_TRIGHTS_5, 		_noop("Expertise Mode")), 
   {0 , NULL, 0}
};

#endif    
